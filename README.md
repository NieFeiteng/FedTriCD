# CDKM / LLoyd / TriCD

MATLAB experiments for CDKM, LLoyd, and TriCD.

Set `CLUSTER_DATA_PATH` to the directory containing dataset `.csv` or sparse `.txt` files. If it is not set, `main_experiment` uses the local `data` directory.
